# Промт перехода — AXIOM / Аксиом (бренд + питч-деки)

Ты продолжаешь работу над брендом и питч-деками **AXIOM** (глоб., латиница) / **«Аксиом»** (RU, кириллица) — рыночная упаковка продукта **ECO SAS** (CDP-ядро `@cdp-us`). Отвечай по-русски.

## ТЕКУЩЕЕ ПОЗИЦИОНИРОВАНИЕ (канон — `docs/AXIOM_PRODUCT_ESSENCE.md`)
AXIOM = **«Ось выручки» для SMB**, ядро — *собрать размазанную клиентскую базу из хаоса (сайт/реклама/CRM/заявки/заказы/таблицы) в один живой актив → показать, кто клиенты, какие каналы работают, кого вернуть → дать действия*. CDP-механика = ЦЕНТР, не модуль. Порядок ценности: база → сегменты → качество каналов → рост → действия → AI (последним). Главный экран — «Сегодня в базе» (карточки-действия). MVP/оффер — «Customer Base Audit за 7 дней». Данные — ТОЛЬКО first-party (no scraping → 152-ФЗ safe). Двойной GTM: SMB напрямую + агентства white-label.

## 🔴 ВАЖНО — ДВА ПРАВИЛА КОПИРАЙТА (юзер настоял, не нарушать)
1. **НЕ писать про деньги напрямую.** ЦА не обсуждает деньги с посторонними. Запрещено: «деньги», суммы выручки/расхода на экране, «по факту оплат / сверено с эквайрингом», ROAS-суммы. Вместо: рост / кого вернуть / качество каналов / клиенты. Оставлены ТОЛЬКО бренд «Ось выручки / Revenue Axis» и «прибыль/profit» в финале-аксиоме. (Был удалён целый слайд «Деньги по факту оплат».)
2. **Тест Кабаныча.** ЦА = **КАБАНЫЧ** — крепкий приземлённый SMB-собственник. Не переваривает абстракции и англицизмы: «ось», «платформа», «доходность», CDP, Revenue OS. «Ось выручки» он НЕ понимает (путает с земной осью, ржёт). Нужна предметность в его словах: «клиенты», «база», «вернуть старых», «не терять заявки». Сильнее ярлыка-категории работает глагол-обещание: «собирает всех ваших клиентов в одну базу и возвращает их».

## ОТКРЫТЫЕ РЕШЕНИЯ ЮЗЕРА (не править деку без них)
1. **Категория-ярлык для Кабаныча НЕ выбрана.** Кандидаты: «Система клиентской базы» / «Программа возврата клиентов» (предметно) vs оставить «Ось выручки» как брендовый зонтик для умных/инвесторов/EN. Думали, не зафиксировали.
2. **Цена** — тарифы заданы как гипотезы (RU Start 4.9-7.9k / Core 12.9-19.9k / Growth 29.9-39.9k / Agency 49.9k+за клиента ₽; US $99 / $249-399 / $699-999 / Agency $399-799+$79-199). Валидировать ресёрчем.

## ДЕКИ (актуальная широкая версия)
- **RU + EN, по 21 слайду**, «Ось выручки», money вычищен, с кейсом. Файлы: `docs/brand/axiom-deck/deck-ru.html`, `deck-en.html` + PDF + `render.js`/`render-en.js` + `fonts/`. Узкая 13-сл RU — `deck-ru-narrow-backup.html` (откат).
- Структура (21): 1 Обложка · 2 Проблема (база в хаосе) · 3 Страх (кто владеет данными = владеет бизнесом; маркетплейс не отдаёт покупателей) · 4 Утешение (база ваша) · 5 Что это (Ось выручки) · 6 **Сегодня в базе** · 7 Собрать · 8 Понять+каналы · 9 Рост в базе→действия · **10 КЕЙС** (RU: WB/Ozon→свой сайт; EN: Amazon/Etsy→own site; «пример сценария») · 11 SEO · 12 Email · 13 Соцразведка · 14 Бронирование · 15 Прогноз · 16 Движок (AI объясняет, не выдумывает) · 17 Для кого · 18 Данные (first-party, no scraping) · 19 Тарифы · 20 Оффер 7 дней · 21 Финал-аксиома + CTA.
- ⚠️ Деки активно правились в **scratchpad** `/private/tmp/.../scratchpad/axiom-deck/` (сессионный!). В репо коммитим из scratchpad. В новой сессии: брать актуальные из `docs/brand/axiom-deck/`.

## РЕНДЕР PDF
- Playwright + headless shell `~/Library/Caches/ms-playwright/chromium_headless_shell-1223/chrome-headless-shell-mac-arm64/chrome-headless-shell`. playwright-модуль был в `.../0b05dba6-.../scratchpad/node_modules/playwright` (может пропасть → `npm i playwright`).
- `node render.js` → AXIOM-deck-RU.pdf (1280×720); массив индексов в скрипте = какие слайды скриншотить. `render-en.js` → EN. Класть на `~/Desktop` + `~/Documents`.
- Проверка: `pdfinfo` + читать PNG глазами (overflow/тофу — частые).

## ДИЗАЙН-СИСТЕМА (Signal Clarity)
- Тёплый near-black + off-white + золото; **clay** для угроз. Lora (дисплей/италик) + Geist Mono (лейблы/цифры) — обе с полной кириллицей.
- ∴ НЕТ в шрифтах → собран из 3 CSS-точек (`.tf`). ▲▼↑→ только в Geist Mono. Мокапы — светлая палитра в браузер-рамке (`.mock`), тень слабая (`box-shadow ...rgba(0,0,0,.4)`; жёсткая = уродливый ореол).
- **Авто-нумерация:** inline `<script>` в конце нумерует футеры + снимает номера-префиксы у кикеров. Перестановки/вставки НЕ ломают номера — руками не нумеровать.
- Грабли вёрстки: `.demo`-слайды → `align-items:start`; длинные `.demo-h` в узкой колонке уменьшать (~30-34px); проверять КАЖДЫЙ тронутый слайд рендером.

## КОНКЛАВЫ (синт-панель 8 персон через Workflow — юзер разрешил Workflow для конклава)
- v1 (лозунги) 0/8 пилотов · v2 (со скринами) relevance 6.3, 0 «да», 7 «может» · v3 (+цена+оффер) 0 «да», 8 «может».
- Находки: «уйдёте — заберёте базу» снимает lock-in; −50% не двигает; обогащение чужими данными = единогласная 152-ФЗ-мина (УБРАНО — теперь first-party only). Полные выводы: tasks/*conclave*.output.

## РЕПО
- `noumenmcp-source/Noumen`, worktree `/Users/a1/cdp-console-square-ui`, ветка `feat/console-square-ui`, FF в main: `git push origin HEAD:refs/heads/main`.
- В main: `docs/AXIOM_PRODUCT_ESSENCE.md` (канон), `docs/BRAND_AXIOM.md`, `docs/NEXT_SESSION_AXIOM.md` (этот файл), `docs/prompts/AXIOM_MARKET_RESEARCH_{validate,discovery}.md`, `docs/brand/axiom-deck/` (деки).

## ПРАВИЛА
- Workflow — только для конклава (разрешено разово); иначе масштаб = Флот, Claude = оркестратор. Перед «готово» — рендер + pdfinfo + скриншот, цитировать. Коммиты заканчивать `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Деки/доки кладём на Desktop по запросу.
