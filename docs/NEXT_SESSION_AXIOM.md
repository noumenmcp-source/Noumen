# Промт перехода — AXIOM / Аксиом (бренд + питч-деки)

Ты продолжаешь работу над брендом и питч-деками **AXIOM** (глоб., латиница) / **«Аксиом»** (RU, кириллица) — рыночная упаковка продукта **ECO SAS** (Revenue OS поверх `@cdp-us`). Отвечай по-русски.

## Что это
AXIOM — «операционная система выручки» (Revenue OS). Не дашборд — слой решений: сводит маркетинг/рекламу/продажи/SEO/CRM/выручку **ПО ФАКТУ ОПЛАТ** (сверка с эквайрингом) и говорит, что приносит деньги, что сливает, что делать. Двойной GTM: SMB напрямую + агентства white-label.

## Координаты
- Бренд-пакет: `docs/BRAND_AXIOM.md` (bilingual positioning/messaging/ToV/audience/boilerplate).
- Деки: `docs/brand/axiom-deck/deck-ru.html` (16 сл) и `deck-en.html` (16 сл, US-локализация) + оба PDF + `render.js`/`render-en.js`, `fonts/`.
- ⚠️ Активно правил в **scratchpad** `/private/tmp/claude-501/-Users-a1/<session>/scratchpad/axiom-deck/` — он сессионный. В НОВОЙ сессии: скопировать deck-*.html, render*.js, fonts/ из репо `docs/brand/axiom-deck/` в свой scratchpad и оттуда рендерить.
- Репо: `noumenmcp-source/Noumen`, worktree `/Users/a1/cdp-console-square-ui`, ветка `feat/console-square-ui`, FF в main: `git push origin HEAD:refs/heads/main`.

## Рендер PDF
- Playwright + headless shell `~/Library/Caches/ms-playwright/chromium_headless_shell-1223/chrome-headless-shell-mac-arm64/chrome-headless-shell`.
- playwright-модуль был в старом scratchpad `.../0b05dba6-.../scratchpad/node_modules/playwright` (может пропасть → `npm i playwright`).
- `node render.js` → `AXIOM-deck-RU.pdf` (1280×720). Массив индексов в скрипте = какие слайды скриншотить для верификации. `render-en.js` → EN. Класть на `~/Desktop` + `~/Documents`.
- Проверка: `pdfinfo` (страницы/размер) + читать PNG-скриншоты глазами (overflow/тофу).

## Дизайн-система (Signal Clarity)
- Тёплый near-black + off-white + золото; **clay** (`--clay`) для угроз. Шрифты **Lora** (дисплей/италик) + **Geist Mono** (лейблы/цифры) — обе с полной кириллицей.
- ∴ НЕТ в шрифтах → собран из 3 CSS-точек (`.tf`). ▲▼↑→ только в Geist Mono (в Lora нет) — использовать только в mono-контексте. Мокапы продукта — светлая палитра в браузер-рамке (`.mock`).
- ⚠️ **Авто-нумерация:** inline `<script>` в конце нумерует футеры по порядку + снимает номера-префиксы у кикеров. Перестановки/вставки слайдов НЕ ломают номера — РУКАМИ НЕ НУМЕРОВАТЬ.
- Грабли вёрстки (частые): продуктовые `.demo`-слайды — `align-items:start` (по верху); длинные `.demo-h` в 360px-колонке уменьшать до ~31-34px; `.mock` box-shadow держать слабым (на тёмном фоне жёсткая тень = уродливый ореол). Проверять КАЖДЫЙ тронутый слайд рендером.

## Структура (16 слайдов)
1 Обложка («Аксиом делает выручку очевидной») · 2 Проблема (95%→25%, чёрный ящик) · **3 СТРАХ** (кто владеет данными — владеет бизнесом; владеете не вы; «арендуете своих клиентов») · **4 РЕЛИФ-пейофф** («Теперь данные — ваши. А значит — и бизнес»; угроза снята; 3 плитки) · 5 Что это (переводит маркетинг в деньги) · 6 Деньги (по факту оплат) · 7 Работает/сливает/делать · 8 SEO · 9 Email · 10 Соцразведка · 11 Бронирование · 12 Прогноз · 13 Движок · 14 Для кого · 15 Результат · **16 Финал-аксиома + CTA** («пилот на ваших данных»).

## Конклавы (синт-панель 8 персон через Workflow — Workflow для конклава юзер разрешил)
- v1 (лозунги) — 0/8 пилотов. v2 (со скринами) — relevance 6.3, 0 «да», 7 «может» (продукт стал виден). v3 (+цена+оффер) — 0 «да», 8 «может» (качество maybe выросло).
- Сильнейший актив: **«по факту оплат, сверка с эквайрингом»** (most_convincing 7/8). Оффер «уйдёте — заберёте базу» снимает lock-in; −50% не двигает (трюк).

## 🔴 ОТКРЫТЫЕ РЕШЕНИЯ ЮЗЕРА (без них деку НЕ править)
1. **Обогащение чужой ЦА vs 152-ФЗ** — конклав ЕДИНОГЛАСНО (8/8) назвал «обогащение данными не-клиентов из соцсетей/рекламы» правовой миной. Решить: убрать из РФ-питча / опция с правовым основанием / переформулировать (агрегаты по ГЕО без ПДн). Сейчас в деке (слайд 4 плитка «Обогащена») ещё есть.
2. **Реальная цена в рублях** + сегментация (соло / точка / агентство-white-label). «Среднерыночная/функций больше» конклав назвал слабым — даёт цифру.
3. **Атрибуция** — переписать слайд движка в «UTM → лид → оплата, сходится с эквайрингом, проверяемо» (атрибуция по лидам через UTM — стандарт, не чёрный ящик).

## Правила
- Workflow — только для конклава (разрешено разово); иначе масштаб = Флот, Claude = оркестратор. Перед «готово» — рендер + pdfinfo + скриншот, цитировать вывод. Коммиты заканчивать `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
