# Core
- Empty workspace project named `cdp`.
- Primary use: research workspace for CDP, client data platforms, and email marketing.
- No application code or runtime entrypoints are present yet.
- Root map:
  - `mem:tech_stack` for languages/tools once introduced.
  - `mem:suggested_commands` for repo-specific commands once defined.
  - `mem:conventions` for naming/style once code or notes start to accrete.
  - `mem:task_completion` for verification commands once a runnable stack exists.
