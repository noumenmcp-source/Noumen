# ⚠️ Старый бокс 137.220.56.211 — НЕ ГАСИТЬ ЦЕЛИКОМ (2026-06-19, проверено)

Промт перехода CDP содержал опасный пункт «погасить старый бокс 137.220.56.211». Это НЕВЕРНО как
сформулировано. Проверено на боевом zavod.dev — что от чего зависит:

## Зависимости zavod.dev (проверено в браузере)
- ТОВАРЫ/каталог: Cloudflare Worker `aiml.pm99lvl.workers.dev` + R2-шарды (хост зашит в странице,
  ~194 товара рендерятся). НЕ на боксе. → при гашении бокса товары НЕ пропадут.
- ПОИСК: `/api/shop/search` отдаёт 200 JSON с данными (работает). Серверный прокси Vercel, бэкенд-хост
  из браузера не виден; по памяти = ES (возможно на этом боксе).
- ОФОРМЛЕНИЕ/RFQ/корзина: `/api/shop/*` → Odoo commerce_os. Бокс 137.220.56.211 = Odoo
  (см. `mem:odoo_server_filestore_incident`: odoo.137-220-56-211.sslip.io). → гашение бокса ломает checkout/RFQ.

## Правильное действие
Погасить ТОЛЬКО устаревший процесс/контейнер v2 CDP-gateway на этом боксе (витрина переключена на новый
gateway cdp.90-156-170-63.sslip.io, v2 больше не нужен). ES + Odoo на боксе ОСТАВИТЬ работать.
Это узкая операция (остановить один старый контейнер), НЕ выключение машины.

⚠️ SSH к 137.220.56.211 у Claude нет (есть только к 90.156.170.63). Перед любым действием на старом
боксе — отдельно получить доступ и инвентаризировать, что на нём крутится (НЕ вслепую).
Доп.: егресс-IP dev-окружения Claude = 137.220.56.211 (контрольный track пришёл с него).

См. `mem:research/cdp-server-deploy-live`, `mem:project_commerce_os_state`.
