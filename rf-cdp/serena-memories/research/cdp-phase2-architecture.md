# CDP Фаза 2 — АРХИТЕКТУРА (2026-06-19). Док: PHASE2_ARCHITECTURE.md

Цель: max-quality email-маркетинг, мультитенант (1 сайт=1 workspace). Всё ПД+движок на ОДНОМ Beget-РФ-боксе
90.156.170.63, AES, без трансграницы, RU-ESP. Заграничное облако — только неперсональное.

## 🔴 ОТКАТ v5 (2026-06-19): Postgres-core ОТМЕНЁН владельцем
Воркфлоу предлагал убрать ES+ClickHouse+Temporal+Dittofeed → Postgres-only (экономия RAM, $0). Владелец:
«такая потеря неприемлема, откатывай». git revert (коммит fa22c77). Движок ОСТАЁТСЯ полным:
Postgres + Elasticsearch + ClickHouse + Dittofeed + Temporal + ingest-gateway + Dex (+Redpanda).
Почему откатили (анализ потерь, см. `mem:research/cdp-phase2-hard-constraints-oss` + транскрипт w2f5vcf30):
теряли масштаб (сотни млн событий), интерактивный OLAP, battle-tested журнеи (Temporal), риск дублей
(pg-boss at-least-once), регресс таргетинга (мис-порт сегментов), потолок на высоконагруженного тенанта.

## ⚠️ ТРИЛЕММА (после отката — НЕ РАЗРЕШЕНА, ждём владельца)
Три требования вместе НЕВОЗМОЖНЫ:
1. Сохранить полный стек ES+CH+Temporal+Dittofeed (подтверждено сейчас).
2. Не апгрейдить RAM — жить на 3.8 ГБ. НО стек УЖЕ OOM'ил ES на 3.8 без доп.нагрузки.
3. Без платного Dittofeed EE. НО мультитенант-Dittofeed = EE (платный).
Одно должно уступить: (A) разрешить апгрейд RAM (8-16ГБ); (B) второй/больший хост (движок вне 3.8ГБ);
(C) принять EE (платно) ради мультитенанта; (D) отказаться от мультитенанта / один инстанс на тенанта.
БЕЗ РЕШЕНИЯ фаза 2 строится на невозможном основании.

## Не спорно (держится): РФ-резидентность (бокс уже Beget РФ, ст.18 локализация ОК), без трансграницы,
RU-ESP, AES (без ГОСТ-СКЗИ — юрист), удаление=DELETE, заграничное облако=только неперсональное.
Pillars качества: доставляемость, durability, интеллект, AI-контент(Флот), журнеи, атрибуция.

См. `mem:research/cdp-phase2-hard-constraints-oss`, PHASE2_DEV_PLAN.md, PHASE2_ARCHITECTURE.md.
