# CDP-US — единая SaaS-экосистема для US B2B

Один аккаунт в облаке → ядро **CDP** (сбор и единый профиль клиента) → подключаемые
модули-апселлы (email, соц-разведка вкл. YouTube/видео, автоматизация, согласие/право).
Рынок: **американские B2B-компании.**

---

## 🔴 ЧАРТЕР СЕГМЕНТАЦИИ (читать первым)

Это **изолированная US-система**. Существует параллельная **RF-система** — она:

- живёт на **других серверах**,
- адаптирована под **законы РФ** (152-ФЗ/РКН),
- **не пересекается** с этой системой, **не знает** о ней, не обменивается данными/кодом/доступами.

В этом репозитории — **только USA**. Никакого 152-ФЗ, РКН, Beget, RU-ESP, РФ-резидентности,
русского клиентского контента. Юр-слой здесь — **только** CCPA/CPRA/CAN-SPAM/TCPA/GDPR.

Подробные правда и правила разделения: [SEGMENTATION.md](SEGMENTATION.md).

---

## Структура

```
cdp-us/
├─ README.md            — этот файл
├─ SEGMENTATION.md      — чартер «RF и USA не смешиваются»
├─ docs/
│  ├─ ARCHITECTURE.md   — архитектура US-экосистемы
│  ├─ ROADMAP.md        — фазы
│  └─ COMPLIANCE-US.md  — юр-слой США (CCPA/CPRA/CAN-SPAM/TCPA/GDPR)
├─ platform/            — аккаунт, мультитенант (OIDC), биллинг, единая консоль
├─ core-cdp/            — ядро данных: ingest → identity → профиль → сегменты
└─ modules/             — подключаемые апселлы
   ├─ email/            — ИИ-email-маркетинг
   ├─ social-intel/     — соц-разведка / SMM-интент (вкл. youtube/ — YouTube/видео)
   ├─ automation/       — автоматизация соцсетей + мессенджеров
   └─ consent/          — согласие и право (US-приватность)
```

## Статус

Фаза 0 — каркас сегментации. Код переносится/пишется по фазам (см.
[docs/ROADMAP.md](docs/ROADMAP.md)). Каждый модуль продаётся как апселл; ядро — вход.

## Быстрый запуск

```bash
pnpm install
pnpm build
pnpm test
```

API без `DATABASE_URL` работает на in-memory stores. Для Postgres:

```bash
cp .env.example .env
pnpm --filter @cdp-us/db db:migrate
pnpm --filter @cdp-us/api dev
```

Основные endpoints:

- `POST /v1/signup`
- `GET /v1/modules`
- `POST /v1/tenants/:tenantId/modules/:moduleKey`
- `POST /v1/track`
- `GET /v1/health`

## Язык

Внутренние док-и — на русском (владелец). **Всё клиентское (UI, лендинги, письма, документы)
— только English** (рынок US). Код/пути/команды не переводятся.
